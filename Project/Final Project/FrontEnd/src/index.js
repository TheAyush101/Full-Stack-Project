import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

// Renders the App component and mount it to the 'root' element in the HTML document

ReactDOM.render(<App />, document.getElementById('root'));


